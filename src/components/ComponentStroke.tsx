import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

type ComponentStrokeProps = {
    
};
const ComponentStroke: React.FC<ComponentStrokeProps> = (props) => {

const {} = props;

  return (
    <View>
      <Text>ComponentStroke</Text>
    </View>
  )
}

export default ComponentStroke

const styles = StyleSheet.create({})