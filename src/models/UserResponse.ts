type UserResponseModel = {
    id?: string,
    name?: string,
    email?: string,
    roles?: string,
    accessToken?: string,
    tokenType?: string,
    refreshToken?: string,
}

export default UserResponseModel;